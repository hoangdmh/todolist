const initState = {
    user: [
        { id: 1, name: 'Hoang' },
        { id: 2, name: 'Hoang Nam' }
    ]
}

const rootReducerReducer = (state = initState, action) => {

    return state;
}

export default rootReducerReducer